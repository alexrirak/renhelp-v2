# Normalize text files to LF in the repository regardless of the
# checking-out OS, so line endings are deterministic across contributors.
* text=auto eol=lf

# Explicitly mark binary files so git never tries to normalize them.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.psd binary
