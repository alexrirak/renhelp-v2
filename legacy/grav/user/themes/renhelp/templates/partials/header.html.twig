<header class="ren-header">
    <div class="ren-logo">
        {% if grav.theme.config.settings.logo %}
            <img src="{{ url(grav.theme.config.settings.logo) }}" alt="Logo" />
        {% else %}
            <h1>{{ site.title }}</h1>
        {% endif %}
    </div>
    {% include 'partials/navigation.html.twig' %}
</header>
